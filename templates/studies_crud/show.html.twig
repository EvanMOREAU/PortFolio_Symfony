{% extends 'base.html.twig' %}

{% block title %}Studies{% endblock %}

{% block body %}
    <h1>Studies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ study.id }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ study.place }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ study.address }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ study.text }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ study.website }}</td>
            </tr>
            <tr>
                <th>Img_path</th>
                <td>{{ study.imgPath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_studies_crud_index') }}">back to list</a>

    <a href="{{ path('app_studies_crud_edit', {'id': study.id}) }}">edit</a>

    {{ include('studies_crud/_delete_form.html.twig') }}
{% endblock %}
